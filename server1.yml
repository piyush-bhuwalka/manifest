service: newpiyushsernew
frameworkVersion: '3.30.1'

provider:
  name: aws
  runtime: java8

# package:
#   artifact: <+artifact.path>  

functions:
  previousTime2new:
    # handler: com.serverless.Handler
    name : {sls:stage}
    image: <+artifact.image>
    events:
      - schedule: ${file(./myCustomFile.yml):globalSchedule}     
      - httpApi:
          path: /time
          method: get
  previousTime3new:
    # handler: com.serverless.Handler
    image: <+artifact.image>
    events:
      - httpApi:
          path: /time1
          method: get  
