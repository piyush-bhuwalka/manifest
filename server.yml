service: newcheckstrylocallyef
frameworkVersion: '3.30.1'

provider:
  name: aws
  runtime: java8
  stackName: custom-stack

# package:
#   artifact: <+artifact.path>  

functions:
  currentTime160:
    handler: com.serverless.Handler
    # image: <+artifact.image>
    events:
      - httpApi:
          path: /time
          method: get
  currentTime161:
    handler: com.serverless.Handler
    # image: <+artifact.image>
    events:
      - httpApi:
          path: /time1
          method: get   
