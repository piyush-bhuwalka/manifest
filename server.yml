service: newcheckstrylocally
frameworkVersion: '3.30.1'

provider:
  name: aws
  runtime: java8
  stackName: custom-stack

# package:
#   artifact: <+artifact.path>  

functions:
  currentTime154:
    # handler: com.serverless.Handler
    image: <+artifact.image>
    events:
      - httpApi:
          path: /time
          method: get
  currentTime155:
    # handler: com.serverless.Handler
    image: <+artifact.image>
    events:
      - httpApi:
          path: /time1
          method: get   
