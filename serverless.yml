service: {{ .Values.serviceName }}
frameworkVersion: '3.30.1'

provider:
  name: aws
  runtime: java8

functions:
  currentTime:
    handler: com.serverless.Handler
    events:
      - httpApi:
          path: /time
          method: get
